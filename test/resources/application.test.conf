include "application.conf"

outbox {
  pollInterval = 100 milliseconds
  batchSize = 10
  maxRetries = 2
  useListenNotify = false
  enableStaleEventCleanup = false
}

slick.dbs.default.db {
  url = "jdbc:postgresql://localhost:5432/outbox_db"
  user = "outbox_user"
  password = "outbox_pass"
  numThreads = 5
  maximumPoolSize = 5
}